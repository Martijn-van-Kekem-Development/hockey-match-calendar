name: Auto update calendar

on:
  schedule:
    - cron: "0 * * * *"
  push:
    branches: [ "main" ]

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
      - run: npm test
      - run: npm start
      - name: Set date
        id: date_step
        run: echo "today=$(date +'%Y-%m-%d %H:%M:%S')" >> $GITHUB_ENV
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload entire repository
          path: './docs'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  #      - uses: stefanzweifel/git-auto-commit-action@v5
#        with:
#          commit_message: "Automatic calendar update on ${{ env.today }} (GMT)"
#          commit_author: "GitHub Actions <actions@noreply.github.com>"
#          commit_user_name: "GitHub Actions"
#          commit_user_email: "actions@noreply.github.com"

#  deploy:
#    needs: run
#    environment:
#      name: github-pages
#      url: ${{ steps.deployment.outputs.page_url }}
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#        with:
#          ref: ${{ github.ref }}
#      - name: Setup Pages
#        uses: actions/configure-pages@v5
#      - name: Upload artifact
#        uses: actions/upload-pages-artifact@v3
#        with:
#          # Upload entire repository
#          path: './docs'
#      - name: Deploy to GitHub Pages
#        id: deployment
#        uses: actions/deploy-pages@v4
