import {Competition} from "../Competition.js";
import { parse } from 'node-html-parser';

export class FIH {
    /**
     * The base URL for FIH TMS.
     * @private
     */
    public static BASE_URL = "https://tms.fih.ch";


}